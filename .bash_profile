# for git 
#export PATH=$PATH:/Desktop/Appli/android-sdk-macosx/tools

# for rbenv
export PATH="$HOME/.rbenv/bin:$PATH"
eval "$(rbenv init -)"

# Get the aliases and functions
if [ -f ~/.bashrc ];
then
     . ~/.bashrc
fi

echo "
=====================================

   I'm on the right track, baby
      I was born this way

  Don't be a drag, just be a queen
 
====================================="
